# dao-vm-loop-a.yml - DISABLED (回退印273 · 已移至01-VM本地实践)
# 如需手动触发: GitHub Actions → dao-vm-loop-a → Run workflow
name: dao-vm-loop-a
on:
  workflow_dispatch:
    inputs:
      duration_min:
        description: 'Keep-alive minutes'
        default: '300'
jobs:
  vm:
    if: false  # 已禁用自动调度 · 仅保留手动触发能力
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
