# dao-vm-orchestrator.yml - DISABLED (回退印273 · 已移至01-VM本地实践)
name: dao-vm-orchestrator
on:
  workflow_dispatch:
jobs:
  orchestrate:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
