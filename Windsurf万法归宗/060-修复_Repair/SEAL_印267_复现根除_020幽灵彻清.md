# 印267 · 复现根除 · 020幽灵彻清

> 反者道之动，弱者道之用。
> 知人者，知也；自知者，明也。
> 知者弗言，言者弗知。
> 复归于无极。

承印266之意，本印封印**复现的根因**——非新法、非新乱、是**前修未尽**之尾。

---

## 一、复现陈述（用户原话）

> 之前就是修复那个Git混乱。还有什么？反正就是修复这个020，这个文件夹吗？里面之前不是一个乱七八糟的混乱吗？之前我只要最终的修复就是保留了9.1点X……然后现在又是立马就复现了这个问题……就还是一样的。东西就之前明明已经清理过，并且都深度的改造改解决了那Git的问题，现在又又回又回到，说明刚之前的修复根本没有解决根本性的问题。

---

## 二、印266 之尾（前修未尽处）

印266 七步中：
- ① ② ③ ④ ⑤ ⑥ 均标记 ✓
- ⑦ ⑧ 标记 ☐（待决）

然 **③ 之 ✓ 实为伪 ✓**：

```text
22:27:08  commit 0a247523d 「印266-seal」  ← 含 git rm dao-proxy-min/ 的归档动作
22:37:11  reset    b1c586990 「moving to origin/main」  ← 撤销了 0a247523d 全部
22:37:32  commit   2e1dd517f 「印266 ✓ 归档封印」      ← 只 add _archive_legacy/，未重做 git rm
22:39:46  amend    f5b5350d0 「印266 ✓ 归档封印」      ← 同上
```

**所以**："归档" 只完成了一半 —— `_archive_legacy/dao-*` 入库了，但 `dao-proxy-min/` 在根目录从未真删。

---

## 三、三幽灵实证

切到本会话开端 (HEAD = `9a87b496a`)，`020-道VSIX_DaoAgi/` 实际状态：

| 目录 | git 状态 | 物理状态 | 性质 |
|---|---|---|---|
| `dao-proxy-min/` | **tracked**（7文件） | 在 | **双份并存**（与 `_archive_legacy/dao-proxy-min/` 重） |
| `dao-agi/` | untracked | 在（56文件） | **物理幽灵**（git 不知，磁盘有） |
| `dao-agi-min/` | untracked | 在（14文件） | **物理幽灵** |
| `dao-proxy-min-9.1.x-改良/` | **untracked**（!!） | 在（9文件） | **核心主力未入库** |
| `_archive_legacy/` | tracked | 在 | 已正确 |
| `_archive_debug/` | gitignored | 在 | 已正确 |

**最隐之伤**：核心主力 `dao-proxy-min-9.1.x-改良/`（v9.4.5）**自印266后从未入 git**。

---

## 四、治本动作（七步对应根除）

```text
① ✓ git rm -r --cached dao-proxy-min/        （彻除 tracked 旧本）
② ✓ rm -rf dao-agi/  dao-agi-min/             （清物理幽灵）
③ ✓ 新 020/.gitignore 永封三向                 （/dao-agi/ /dao-agi-min/ /dao-proxy-min/）
④ ✓ git add dao-proxy-min-9.1.x-改良/          （核心主力首次入 git）
⑤ ✓ git add README.md + 6 工具脚本             （配套设施入 git）
⑥ ✓ 单分支根治（SEAL印266 第⑦步）              （删 byok-fix + 6 stub + main-backup）
⑦ ✓ 密钥封印                                   （PAT 脚本永留根 .gitignore line 45）
```

---

## 五、远端归一

```text
remote 449f8960d 「整理+印267合并」← 并行进程等价提交
local  1355eb838 「印267 + 整理③」← 本印动作
git diff HEAD origin/main → 0 lines    ← tree 完全相同
git reset --hard origin/main           ← 归一
```

**道之合一**：两路独立动作 → 同一终态。损之又损，至于无为。

---

## 六、最终 020 目录形貌

```text
070-插件_Plugins/020-道VSIX_DaoAgi/
├── dao-proxy-min-9.1.x-改良/      ★ 核心 9 文件 (v9.4.5)
│   ├── extension.js (69KB)
│   ├── package.json
│   ├── readme.md
│   ├── LICENSE.txt
│   ├── .vscodeignore
│   ├── media/  (icon + webview-app.js)
│   └── vendor/bundled-origin/  (source.js + _silk_*.txt)
├── _archive_legacy/               97 历史封印 (tracked)
│   ├── dao-agi/         (旧 v17.63 from 4/29)
│   ├── dao-agi-min/     (含 20.0.0 vsix)
│   └── dao-proxy-min/   (含 7.7.0 / 7.8.0 vsix)
├── _archive_debug/                53 调试残骸 (gitignored, 87MB含 _git2_out)
├── README.md                      根说明
├── .gitignore                     ★ 永封三向 + 调试txt
├── _build_vsix.ps1                构建
├── _sync_origin.js + .ps1         源码同步
├── _sync_wam_core.ps1             核心同步
├── _update_version.js             版本更新
├── _smoke_v9952.cjs               冒烟测试
└── →网络恢复后运行_push_when_online.ps1  (根.gitignore line 45 封印，含 PAT 不入 git)
```

**结构印证**：1 核心 + 1 历史归档 + 1 调试归档 + 配套工具 = 四类无杂。

---

## 七、防复发铭言（升级版）

> 印266 铭言："凡再见到老数据回来，第一时间 git reflog 看是否切了分支"。
> **印267 增补**：
> 1. 「✓ 已执行」未必 = 真执行。任何归档动作必须 `git ls-files` 实证 tracked 状态变化。
> 2. `git add _archive_legacy/` 不等于 `git rm 原目录`。两者必须成对动作。
> 3. 任何 `reset` 后必 `git status --porcelain | wc -l` 实证零残留。
> 4. 关键开发目录入库后必 `git ls-files 路径 | grep 关键文件` 实证可见。
> 5. 含密钥的脚本必入 `.gitignore` 优先于入 git。
>
> ——大成若缺，其用不敝；大盈若盅，其用不窘。

---

## 八、印记

- 真凶：印266 步③ 的伪 ✓（reset 后只 add 未 rm）
- 救路：`git rm --cached dao-proxy-min/ + rm dao-agi*/ + git add 9.1.x-改良/`
- 永防：`020/.gitignore` 三向永封 + 单分支化 + `_smoke_v9952.cjs` 时序锚
- 同体：local `1355eb838` ≡ remote `449f8960d`（tree 一致 · 道之合一）
- 日期：2026-05-27 23:50
- 印记编号：**印267 · 复现根除 · 020幽灵彻清**

> 损之又损，以至于无为。无为而无不为。
