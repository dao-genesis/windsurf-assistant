# windsurf-assistant 宏观原理

> 不写代码细节，只讲整体思想和运行原理。

---

## 一、这个项目是做什么的

它是一个围绕 **Windsurf / Devin Desktop** 这类 AI 编程 IDE 构建的工具集。核心思想是：

> 官方 IDE 把模型、账号、提示词都封装在封闭管道里，这个项目在管道外面包了一层"可自己控制的壳"。

让你可以：
- 接入自己的第三方模型 API
- 管理多个账号
- 调整系统提示词
- 把 IDE 的 AI 能力拆出来放到别的地方用

---

## 二、核心要解决什么问题

### 问题 1：模型选择不自由
官方 IDE 只能用官方提供的模型，不能换成自己的 API。

### 问题 2：账号额度受限
一个账号额度用完了，只能手动换账号或付费升级。

### 问题 3：提示词不可控
用户看不到系统提示词（system prompt），更无法修改。

### 问题 4：IDE 绑定太死
想在一些场景下脱离 IDE 使用它的 AI 能力，很困难。

---

## 三、整体架构：一气化三清

项目把自己拆成三个互相独立的模块，每个模块解决一个问题：

```
              Windsurf / Devin Cloud
                     ↑
        ┌────────────┼────────────┐
        ▼            ▼            ▼
    反代 API      切号 WAM      提示词反代
   (dao-core)    (packages/wam)  (dao-proxy-min/pro)
        │            │            │
        ▼            ▼            ▼
   任意 OpenAI     多账号轮换     自定义系统提示词
     客户端      （额度感知）      + 外接模型
```

### 反代 API
把官方云端模型包装成标准 OpenAI 接口。任何支持 OpenAI API 的软件都可以调用它。

### 切号 WAM
在多个 Windsurf 账号之间自动切换，哪个有额度就用哪个。

### 提示词反代
截获 IDE 发给云端的请求，在请求里替换/注入系统提示词，同时保留工具调用能力。

---

## 四、两个关键实现思路

### 4.1 本地代理

整个项目很多地方都用到了一个基础技术：**本地 HTTP/HTTPS 代理**。

原理很简单：
1. IDE 本来直接连官方服务器
2. 项目让 IDE 改连本地的一个代理端口（比如 `127.0.0.1:8937`）
3. 代理收到请求后，可以做三件事：
   - 原样转发给官方服务器（透传）
   - 修改后再转发（改提示词、换模型）
   - 直接转给第三方 API（外接模型）

所有操作都在你本地完成，云端只能看到来自你电脑的流量。

### 4.2 拦截与改写 RPC

Windsurf 不是用普通 HTTP API，而是用一套私有 RPC（远程过程调用）协议和云端通信。

项目做的事：
1. 搞懂这套 RPC 的数据格式
2. 在本地搭一个"翻译官"
3. 把 RPC 请求翻译成标准 OpenAI 格式，再转发给第三方模型
4. 拿到第三方模型的回复后，再翻译回 RPC 格式还给 IDE

---

## 五、四个模块分别怎么工作

### 5.1 反代 API（dao-core）

运行环境：你自己的服务器/VPS/本地电脑。

工作流程：
```
你的其他客户端 (LobeChat/Aider/Cursor)
           ↓
      调用 OpenAI API
           ↓
    你的 dao-core 守护进程
           ↓
      转译成 Windsurf RPC
           ↓
       Windsurf 官方云端
```

意义：让 Windsurf 的模型能力脱离 Windsurf IDE，可以在任何软件里用。

### 5.2 切号 WAM（packages/wam）

这是一个 VS Code 扩展。

工作流程：
1. 你提前在配置文件里存好几个 Windsurf 账号的 token
2. WAM 定期检查每个账号的剩余额度
3. 当前账号额度快用完时，自动切换到另一个有额度的账号
4. 避免一个账号用完就卡住

### 5.3 提示词反代（dao-proxy-min / dao-proxy-pro）

也是 VS Code 扩展，但更偏"中间人"。

工作流程：
```
IDE Cascade 面板
     ↓
 发起 RPC 请求
     ↓
 dao-proxy-pro 本地拦截
     ↓
 修改 system prompt / 替换目标模型
     ↓
 发给官方或第三方模型
     ↓
 拿到结果再包装回 RPC 给 IDE
```

dao-proxy-pro 比 min 多了"外接第三方 API"功能：不是简单改提示词，而是把请求直接路由到 DeepSeek/OpenAI 等模型。

### 5.4 Devin Desktop 插件版（dao-desktop）

这个模块最特殊：它把官方 Windsurf 扩展本体"拆"出来，装进普通 VS Code 或 Devin Desktop 里当插件用。

目的：
- 不依赖官方 IDE 的私有 API
- 在任何 VS Code 系编辑器里都能获得类似 Windsurf 的 Cascade 面板
- 和前面三个模块组合使用

---

## 六、数据怎么流动（以 Proxy Pro 为例）

你现在正在用的就是这条链路：

```
[Devin Desktop 的 Cascade 面板]
            ↓
[选择 SWE-1.6 Fast/Slow]
            ↓
[dao-desktop 插件里的 proxy-pro.js]
            ↓
[查路由表：MODEL_SWE_1_6_FAST → DeepSeek v4-flash]
            ↓
[把请求转成 DeepSeek API 格式]
            ↓
[发到 api.deepseek.com]
            ↓
[DeepSeek 返回结果]
            ↓
[包装回 Windsurf RPC 格式]
            ↓
[显示在 Cascade 聊天窗口]
```

对你而言，界面还是 Windsurf 的界面；对 DeepSeek 而言，它只是收到了一个普通 API 调用。

---

## 七、安全边界

项目强调"去中心化"和"自托管"，意思是：

| 数据 | 存在哪里 | 谁能看到 |
|------|---------|---------|
| GitHub 访问令牌 | 浏览器本地 | 只有你 |
| 配置文件（账号、API Key、提示词） | 你自己的 GitHub Gist | 你 + 持有你 PAT 的人 |
| API Key | 你的本地文件 | 只有你 |
| 实际对话内容 | 你的电脑 ↔ 模型供应商 | 你和模型供应商 |

上游仓库只负责分发代码，不接触你的数据。

---

## 八、宏观上能学到什么

1. **代理思维**：很多复杂系统的问题，都可以通过"在中间加一层代理"来解决。
2. **协议翻译**：学会一种协议后，可以把它包装成另一种更通用的协议。
3. **本地优先**：敏感配置和关键逻辑尽量放在本地，减少对云服务的信任。
4. **模块化设计**：一个大系统拆成几个互相独立的小模块，每个模块只负责一件事。
5. **逆向工程的价值**：理解私有协议后，可以做出比官方更灵活的工具。

---

## 九、名词速查

| 名词 | 含义 |
|------|------|
| VSIX | VS Code 插件的安装包格式 |
| RPC | 远程过程调用，IDE 和云端之间对话的协议 |
| SSE | 服务器发送事件，AI 回复流式输出的技术 |
| system prompt | 系统提示词，决定模型身份和行为 |
| model UID | 官方给模型起的内部名字 |
| provider | 模型供应商，比如 DeepSeek、OpenAI |
| route / 路由 | 把某个官方模型映射到某个第三方模型 |
| origin invert | 拦截并改写 IDE 的原始请求 |
| passthrough | 原样透传，不做修改 |

---

## 十、一句话总结

> 这个项目本质上是一套"本地中间件"：站在 AI 编程 IDE 和云端模型之间，把原本封闭的管道拆开，让你能换模型、换账号、改提示词、复用能力。
