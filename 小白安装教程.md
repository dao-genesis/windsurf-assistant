# Devin 破甲教程

> 全程免费，用 DeepSeek API 代替 Windsurf/Devin 付费模型。
> 耗时约 30 分钟。零代码基础可操作。
> 破甲目前更合适国产模型,GPT系列不合适。
> 原开源作者项目中还有其它工具,如多账号切号等,可参考项目说明。
> 项目简单原理在另外一个文档:宏观原理.md 中。原作者在项目中大量使用道德经之类的内容,所以项目阅读起来有一定难度,所以可以先阅读这个文档。
---

## 前置准备一览

| 需要的东西 | 在哪里获取 | 费用 |
|-----------|-----------|--------|
| Devin Desktop（原 Windsurf） | codeium.com 下载 | 免费 |
| Node.js | nodejs.org 下载 LTS 版 | 免费 |
| DeepSeek API Key | platform.deepseek.com 注册 | 新用户送额度，后续按量付费（很便宜） |
| windsurf-assistant 源码 | GitHub 下载 ZIP | 免费 |

---

## 第一步：确认基础环境

### 1.1 安装 Node.js

去 `https://nodejs.org` 下载 **LTS 版本**（左边那个绿色按钮）。

安装时一路点 Next，全部默认即可。

### 1.2 验证安装

打开 **命令提示符**（按 `Win+R`，输入 `cmd`，回车），输入：

```cmd
node --version
```

如果显示类似 `v22.14.0`，说明装好了。

### 1.3 安装 Devin Desktop

去 `https://codeium.com` 下载 Devin Desktop（原 Windsurf）。

安装后打开，注册/登录一个账号。这个账号是免费的，不用付费。

---

## 第二步：获取 DeepSeek API Key

### 2.1 注册 DeepSeek

打开 `https://platform.deepseek.com`，注册账号。

### 2.2 创建 API Key

登录后，左侧菜单 → **API Keys** → **创建 API Key**。

随便起个名字，比如 `devin`。创建后会显示一串 `sk-` 开头的密钥。

**立刻复制保存到记事本！** 这个 Key 只显示一次，关掉就看不到了。

### 2.3 充值（可选但建议）

新用户送约 10 元额度。如果不够用，充值 10-20 元就够用很久。DeepSeek 非常便宜。

---

## 第三步：下载并解压项目

### 3.1 下载

打开 `https://github.com/dao-genesis/windsurf-assistant`

点击绿色 **Code** 按钮 → **Download ZIP**。

### 3.2 解压

把下载的 ZIP 文件解压到桌面。

解压后应该看到：`windsurf-assistant-main/windsurf-assistant-main/` 两层目录。

**记下完整路径**，类似：`C:\Users\你的用户名\Desktop\windsurf-assistant-main\windsurf-assistant-main`

---

## 第四步：构建插件（只需构建 dao-proxy-pro）

### 4.1 打开命令提示符

`Win+R` → 输入 `cmd` → 回车。

> **为什么用 CMD 而不是 PowerShell？**  
> Windows 的 PowerShell 默认禁止运行脚本，`npm` 会报 `无法加载文件 npm.ps1`。用 CMD 直接跳过这个问题。

### 4.2 进入项目目录

```cmd
cd C:\Users\你的用户名\Desktop\windsurf-assistant-main\windsurf-assistant-main
```

> 把"你的用户名"换成实际的。路径可以在文件管理器地址栏复制后粘贴。

### 4.3 安装打包工具（只需执行一次）

```cmd
npm install @vscode/vsce --save-dev
```

会显示很多日志，等它跑完（约 1 分钟）。

### 4.4 构建 dao-proxy-pro

```cmd
node scripts/build-vsix.js dao-proxy-pro
```

看到 `DONE Packaged: ...dao-proxy-pro-9.9.358.vsix` 就成功了。

**记住这个文件路径：**
```
C:\Users\你的用户名\Desktop\windsurf-assistant-main\windsurf-assistant-main\packages\dao-proxy-pro\dao-proxy-pro-9.9.358.vsix
```

> **只构建 dao-proxy-pro 就够了。**
>
> 这个项目中其他插件的作用：
> - `dao-desktop`：给普通 VS Code 提供 Cascade 面板。**Devin Desktop 已有此功能，装了反而冲突**
> - `dao-proxy-min`：Pro 的精简版，功能已被 Pro 包含
> - `wam`：多账号切换，单号用户不需要
>
> **普通用户只需一个 dao-proxy-pro。**

---

## 第五步：配置网络代理（国内用户必须）

> **这是国内用户最容易卡住的一步。**  
> Devin Desktop 依赖 Firebase 认证和 Codeium 服务器，这些服务在国内被墙。  
> 普通的浏览器代理只覆盖网页，覆盖不到 Devin Desktop 的本地程序。

### 5.1 你需要一个有 TUN 模式的代理

**TUN 模式** = 创建虚拟网卡，让所有程序（不只是浏览器）的流量都走代理。

推荐工具（二选一）：

| 工具 | 下载 | 特点 |
|------|------|------|
| **Clash Verge** | github.com/clash-verge-rev/clash-verge-rev/releases | 界面现代，TUN 一键开启 |
| **Clash for Windows** | github.com/Fndroid/clash_for_windows_pkg/releases | 老牌工具，稳定 |

### 5.2 导入机场订阅

1. 安装后打开 Clash，找到 **配置/Profiles**
2. 粘贴你的机场订阅链接（快柠檬后台可以找到）
3. 下载配置

### 5.3 开启 TUN 模式

- **Clash Verge**：设置 → 打开 **TUN 模式**
- **Clash for Windows**：Settings → Service Mode → Install → 打开 **TUN Mode**

系统会弹出 UAC 提示，点"是"（需要管理员权限）。

### 5.4 选择节点

**推荐：美国西海岸**（洛杉矶/圣何塞）或 **日本东京**。

> 不建议用香港节点——Codeium 经常把它识别为受限地区。

### 5.5 验证 TUN 是否生效

打开 CMD，输入：

```cmd
curl https://www.google.com
```

如果有返回内容（不是超时），说明 TUN 生效。

### 5.6 重启 Devin Desktop

**先确保 TUN 开着**，再打开 Devin Desktop。登录认证应该能顺利通过。

> 如果 agent 还是连不上（显示 "No agents connected"），换一个节点再试。同一个地区的不同节点，可能一个通一个不通。

---

## 第六步：安装插件到 Devin Desktop

### 6.1 安装 VSIX

1. 打开 **Devin Desktop**
2. 按 `Ctrl+Shift+P`
3. 输入 `Install from VSIX`，选择 `Extensions: Install from VSIX...`
4. 选择第四步构建的 `dao-proxy-pro-9.9.358.vsix`

### 6.2 重启 Devin Desktop

按 `Ctrl+Shift+P` → `Reload Window`。

### 6.3 确认安装成功

左侧活动栏出现 **"道Agent Pro"** 图标。

点开有四个标签页：
- ① 本源（设置/源码）
- ② 渠道配置
- ③ 模型路由
- ⑤ 模型反代

---

## 第七步：配置 Proxy Pro

### 7.1 打开道Agent Pro 面板

点击左侧活动栏的 **"道Agent Pro"** 图标。

### 7.2 添加渠道（配置 DeepSeek）

1. 切到 **"② 渠道配置"** 标签
2. 点 **"添加渠道"**
3. 选择 **DeepSeek**
4. 粘贴你的 API Key（`sk-` 开头那串）
5. 点确定

面板显示 DeepSeek **在线**，下方出现识别到的模型：`deepseek-v4-flash`、`deepseek-v4-pro`。

### 7.3 配路由（把官方模型指向 DeepSeek）

切到 **"③ 模型路由"** 标签：

1. 点 **"配路由"**
2. 在弹出的输入框输入官方模型 UID：`MODEL_SWE_1_6_FAST`
3. 按回车
4. Provider 选 **DeepSeek**，外接模型选 **deepseek-v4-flash**

### 7.4 再配一条 Slow 的路由

重复 7.3，把 UID 改成：`MODEL_SWE_1_6`，同样选 **deepseek-v4-flash**。

### 7.5 配置效果

| 在 Cascade 中选择的模型 | 实际调用 | 费用来源 |
|------------------------|---------|---------|
| SWE-1.6 Slow | DeepSeek deepseek-v4-flash | 你的 DeepSeek Key |
| SWE-1.6 Fast | DeepSeek deepseek-v4-flash | 你的 DeepSeek Key |

---

## 第八步：验证是否生效

### 8.1 打开 Cascade 面板

按 `Ctrl+Shift+P`，输入 `Cascade`，打开 Cascade 面板。

### 8.2 新建对话

点 `+` 新建会话。模型选择 **SWE-1.6 Slow**（或你配了路由的模型）。

### 8.3 随意聊天

随便发几条消息，能正常回复就行。

### 8.4 看用量（最可靠验证）

打开 `https://platform.deepseek.com` → 用量/Usage 页面。

如果 token 数在涨，说明成功，**已经在用 DeepSeek 了。**

> 注意：Cascade 里的 AI 可能会说"我是 SWE-1.6 Slow"，这不代表路由没生效。  
> 系统提示词让它扮演这个角色，但它背后的模型已经是 DeepSeek。  
> **看 DeepSeek 控制台用量是唯一可靠的验证方式。**

---

## 常见问题

### Q1：Devin Desktop 连不上 agent（No agents connected / 一直 Connecting）

**根因：网络被墙。** 回到第五步检查：
1. TUN 模式是否开着
2. 换一个节点（美国/日本），不要用香港
3. 重启 Devin Desktop

### Q2：提示 "Permission denied" 或模型不可用

换模型。在 Proxy Pro 面板里点那条路由，把模型从 `deepseek-v4-flash` 换成 `deepseek-v4-pro`。

### Q3：提示 "context deadline exceeded"

DeepSeek 服务器忙。等几分钟重试，或换另一个模型。

### Q4：提示"模型未配置反代通道"

API Key 没填或渠道没启用。去 **② 渠道配置** 确认 DeepSeek 显示在线。

### Q5：回复说自己是 "SWE-1.6 Slow"

这不代表路由没生效。系统提示词让它扮演这个角色，背后模型已是 DeepSeek。看 DeepSeek 用量验证。

### Q6：Cascade 面板找不到

确认完全重启过 Devin Desktop（Alt+F4 关掉再开），且道Agent Pro 插件已安装。

### Q7：npm 报错 "无法加载文件 npm.ps1"

用 **CMD**（命令提示符），不要用 PowerShell。`Win+R` → `cmd` → 回车。

---

## 花费总结

| 项目 | 费用 |
|------|------|
| Devin Desktop | 免费 |
| windsurf-assistant | 免费开源 |
| DeepSeek API | 新用户送 10 元，后续约 1 元 / 百万 token |
| 总计 | 几乎免费 |

---

## 附：如果你想换其他模型

Proxy Pro 支持这些渠道：

| 渠道 | 注册地址 | 备注 |
|------|---------|------|
| DeepSeek | platform.deepseek.com | 推荐，最便宜 |
| 硅基流动 | cloud.siliconflow.cn | 国产，免费额度多 |
| OpenRouter | openrouter.ai | 聚合平台，模型最全 |
| OpenAI | platform.openai.com | 最贵，能力最强 |
| Groq | console.groq.com | 极速，有免费额度 |

配置方式都一样：添加渠道 → 填 Key → 配路由。
